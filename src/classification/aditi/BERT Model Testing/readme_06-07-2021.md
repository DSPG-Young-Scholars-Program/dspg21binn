This folder contains information on all BERT types and classification model types that will be tested prior to executing analysis on all 1 millionn articles. These model combinations will be tested on the training dataset of 600 articles.

This folder includes:
Neural network using BERT, DistilBert, and RoBERTa
Logistic regression using BERT
Dataset saved as an .xlsx and a .csv